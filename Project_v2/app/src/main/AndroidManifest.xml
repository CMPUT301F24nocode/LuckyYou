<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjectV2"
        tools:targetApi="31">

        <activity
            android:name=".View.EventHomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".View.NotificationActivity" />
        <activity android:name=".View.ProfileActivity" />
        <activity android:name=".View.FacilityListActivity" />
        <activity android:name=".View.AdminProfileListActivity" />
        <activity android:name=".View.AdminImageListActivity" />
        <activity android:name=".View.AdminFacilityListActivity" />
        <activity android:name=".View.CreateEventOptionsActivity" />
        <activity android:name=".View.EventCreatorActivity" />
        <activity android:name=".View.EventLandingPageActivity" />
        <activity android:name=".View.EventOptionsActivity" />
        <activity android:name=".View.FacilityListActivity"
            tools:ignore="DuplicateActivity" />
        <activity android:name=".View.FacilityCreateEditActivity" />
        <activity android:name=".View.EventDetailsActivity"/>

    </application>

</manifest>